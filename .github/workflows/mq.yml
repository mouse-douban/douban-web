name: Non-Trivial Checks

on:
  merge_group:
    types:
      - checks_requested

jobs:
  fake:
    name: Checks
    runs-on: ubuntu-latest
    steps:
      - run: sleep 15
      - run: echo "Success"
